
!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title Gohan2
    = csrf_meta_tags
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
    = stylesheet_link_tag    'posts.show', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'

.wrapper
  .view
    = @post.view

  .chat-area

    .post-detail
      .post__shopname
        = @post.shopname
      .post__image
        = @post.image
      .post__content
        = @post.content
      .post__sender
        = @post.user.nickname
      .post__edit
        - if current_user.id == @post.user.id
          = link_to "投稿を編集する", edit_post_path(@post)
      .post__likes
        いいね件数：
        = @post.likes.count

    .like-create
      -if current_user.already_liked?(@post)
        = button_to 'いいねを取り消す', post_like_path(@post), method: :delete
      -else
        = button_to 'いいね', post_likes_path(@post)

    .liked-user
      -if @post.likes.count > 0
        いいねしたユーザー
        - @post.liked_users.each do |user|
          = user.nickname

    .comment
      .comment__form
        - if current_user
          = form_for [@post, @comment] do |f|
            = f.text_field :content, class: 'form__message', placeholder: 'コメントを入力してください'
            = f.submit 'Send', class: 'form__submit'
     
      .comment__index  
        - @comments.each do |comment|
          = link_to "#{comment.content}/#{comment.user.nickname}", edit_post_comment_path(comment.post.id, comment.id)