<%# -# -if current_user.already_liked?(posts)
-#   = button_to post_like_path(posts), class:"like_selsect", method: :delete, remote: true do
-#     %i.like.fas.fa-heart
-#     = "#{posts.likes.count}　　#{posts.shopname}"
-# -else
-#   = button_to post_likes_path(posts), class:"like_selsect",remote: true do
-#     %i.dislike.far.fa-heart
-#     = "#{posts.likes.count}　　#{posts.shopname}"


.like-link{:id => "like-link-#{post.id}"}
  - if current_user.likes.find_by(post_id: post.id)
    = link_to unlike_path(post.id), method: :delete, remote: true do
      .iine__button
        ❤️#{post.likes.count}
  - else
    = link_to like_path(post.id), method: :post, remote: true do
      .iine__button
        ♡️#{post.likes.count} %>


<div class="like-link" id="like-link-<%= @post.id %>">
  <% if current_user.likes.find_by(post_id: @post.id) %>
    <%= link_to unlike_path(@post.id), method: :delete, remote: true do %>
        <div class = "iine__button">❤️<%= @post.likes.count %></div>
    <% end %>
  <% else %>
    <%= link_to like_path(@post.id), method: :post, remote: true do %>
        <div class = "iine__button">♡️<%= @post.likes.count %></div>
    <% end %>
  <% end %>
</div>