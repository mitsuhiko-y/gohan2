!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title GoFun
    = csrf_meta_tags
    = stylesheet_link_tag    'application', media: 'all'
    = stylesheet_link_tag    'user.edit', media: 'all'
  %body

    .about_page
      %i.edit_icon.far.fa-edit
      %h.about_icon アカウント編集ページです。

    = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
      = render "devise/shared/error_messages", resource: resource
      .field
        = f.text_field :nickname, autofocus: true, autocomplete: "nickname", placeholder: 'ニックネーム', rows: "3"
      .field
        = f.file_field :image, autofocus: true, autocomplete: "image", placeholder: 'プロフィール写真'
      .field
        = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'メールアドレス'
      .field
        = f.password_field :password, autocomplete: "new-password", placeholder: '新パスワード（6文字以上)'
      .field
        = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: '新パスワードを再入力してください(確認用)'
      .field
        = f.password_field :current_password, autocomplete: "current-password", placeholder: '現在のパスワード'
      .actions
        = f.submit "Update"

    .footer
      .back
        = link_to "<Back", :back
      .destroy
        %a.btn.btn-danger{"aria-label" => "Delete", :href => "/users_delete", "data-method" => "get", "data-confirm" => "このアカウントを削除してよろしいですか?"}
          %i.far.fa-trash-alt{"aria-hidden" => "true", :title => "このアカウントを削除しますか？"}
          %h.about_delete_button 削除する
      
