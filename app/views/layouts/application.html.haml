!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title Gohan2
    = csrf_meta_tags
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    
  %body
    .wrapper
      .header
        .header_set
          - if user_signed_in?
            = link_to image_tag("/images/GoFun_icon.png", alt: "ロゴ", width: "40px"), user_path(current_user.id)
            = link_to "#{current_user.nickname}", user_path(current_user.id)
            = link_to "タイムライン", posts_path
            = link_to "新規投稿", new_post_path
            %a{"data-method" => "delete", :href => "/users/sign_out", :rel => "nofollow"} サインアウト


          - else
            = link_to image_tag("/images/GoFun_icon.png", alt: "ロゴ", width: "40px"), new_app_path
            = link_to "ログイン", new_user_session_path
      .main
        = render 'layouts/notifications'
        = yield